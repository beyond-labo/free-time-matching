name: OpenAPI contract
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  not-configured:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Explain missing configuration
        run: |
          echo 'OpenAPI 生成、lint、互換性、サンプル検証が未実装です。' >&2
          exit 1
