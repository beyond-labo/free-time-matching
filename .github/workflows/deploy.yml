name: Backend deployment
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  not-configured:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Explain missing configuration
        run: |
          echo '公開先、認証、検証済み配布物のデプロイ手順が未実装です。' >&2
          exit 1
