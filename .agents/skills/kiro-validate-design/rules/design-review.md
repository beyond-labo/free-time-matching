# 設計レビューの観点

- 要件の各受け入れ条件に、具体的な設計と検証方法が対応している。
- 責務、依存方向、公開契約が既存コードと関連仕様に整合する。
- 型と入力境界、失敗処理、データの扱いが明確である。
- 実装に必要な前提が実在し、複雑さが目的に見合っている。
- 現行の設計に古い判断が残らず、判断の根拠をたどれる。

指摘は問題、影響、修正案、要件ID、設計の該当箇所で示す。
すべてのブロッカーを記録し、提案と分ける。
`GO` は未解決のブロッカーがなく実装方針が成立するとき、`NO-GO` は成立しないときに返す。
確認できない必須事項は未確認と明示する。
